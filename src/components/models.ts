export interface Todo {
  id: number;
  content: string;
}

export interface Meta {
  totalCount: number;
}

export interface Data {
  name : string;
}

export interface Val {
  id : boolean
}

export interface IsFileLoaded {
  id : boolean
}